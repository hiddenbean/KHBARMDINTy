<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Khbar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/darker_gray</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@android:color/white</item>
    </style>

    <!-- No title no action bar application theme. -->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorControlHighlight">@color/colorAccentLight</item>
    </style>

    <!-- No title no action bar application theme. -->
    <style name="AppTheme.Home" parent="AppTheme.NoActionBar">
        <item name="android:background">#fafafa</item>
        <item name="colorControlHighlight">@color/colorAccentLight</item>
    </style>

    <!-- Primary button theme. -->
    <style name="PrimaryButton" parent="AppTheme">
        <item name="colorButtonNormal">@color/colorPrimary</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- White button theme. -->
    <style name="WhiteButton" parent="AppTheme">
        <item name="colorButtonNormal">@android:color/white</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- Facebook button theme. -->
    <style name="FacebookButton" parent="AppTheme">
        <item name="colorButtonNormal">@color/colorFacebook</item>
        <item name="colorControlHighlight">@color/colorFacebookDark</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
